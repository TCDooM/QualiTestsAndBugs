kind: blueprint
spec_version: 1
metadata:
  tags:
    - test-key1: test_value1
    - test-key2: test_value2
clouds:
- aws: eu-west-1

applications:
- no-config-script-app

services:
- terraform-publisher  # creates aws resources to be tagged

debugging:
  bastion_availability: enabled-on
